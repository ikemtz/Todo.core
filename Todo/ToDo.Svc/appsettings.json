{
  "ConnectionStrings": {
    /*If running on linux you must use a user name and password, integrated security will not work*/
    "ToDoDatabase": "Server=localhost;Database=ToDo;User=TodoUser;Trusted_Connection=true;"
  },
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  }
}
